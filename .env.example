# 复制为 .env 并填入你自己的 key。.env 不要提交到版本库。
# 只需填你要用的那一个（也可以在应用里「设置 → Provider」直接填，会写回这个文件）。
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
# DeepSeek（走官方 Anthropic 兼容端点 https://api.deepseek.com/anthropic）
DEEPSEEK_API_KEY=sk-...
# Kimi / Moonshot（https://api.moonshot.cn/v1）
MOONSHOT_API_KEY=sk-...
# 火山引擎方舟 coding 端点（https://ark.cn-beijing.volces.com/api/coding，需 CodingPlan 订阅）
ARK_API_KEY=ark-...

# ---- 检索源（不是模型 provider，没有也能用：免 key 的 bing+ddg 链路永远可用）----
# Firecrawl 托管检索源（https://firecrawl.dev）。**只从环境读，不进 config.yaml**。
# 有 key 时 config.yaml 的 web.firecrawl 才生效（默认 primary = 主搜）；没 key 一律等于 off。
FIRECRAWL_API_KEY=fc-...
